# Artemis API key (fallback only - primary data from MicroStrategy API)
VITE_ARTEMIS_API_KEY=your_api_key_here

# Artemis datasvc token seed (ROT13 encoded) - for preferred stock prices
# Get this from artemis-dashboard-frontend/.env or ask an admin
ARTEMIS_ENCRYPTED_TOKEN_SEED=snxr_frperg
